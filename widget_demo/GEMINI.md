# Project: widget_demo

## Project Overview

This is a new Flutter project named `widget_demo`. It appears to be the default "counter" application that is generated when a new Flutter project is created.

The main application logic is in `lib/main.dart`. The project uses the standard Flutter SDK and includes `cupertino_icons` for iOS-style icons. For development, it uses `flutter_test` for testing and `flutter_lints` for code analysis.

## Building and Running

To build and run this application, use the following standard Flutter commands:

*   **Get dependencies:**
    ```bash
    flutter pub get
    ```

*   **Run the application:**
    ```bash
    flutter run
    ```

*   **Run tests:**
    ```bash
    flutter test
    ```

## Development Conventions

The project follows the standard Flutter and Dart conventions.

*   **Linting:** The project uses the `flutter_lints` package, with the rules enabled in `analysis_options.yaml`.
*   **Testing:** Widget tests are located in the `test/` directory, with `test/widget_test.dart` as the default test file.
